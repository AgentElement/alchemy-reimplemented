use lambda_calculus::Term;

pub fn btree(n: u32, freevars_count: u32) -> Option<Term> {
    None
}

pub fn fontana(abs_range: (f64, f64), 
               app_range: (f64, f64), 
               depth_cutoff: u32, 
               freevars_count: u32) -> Option<Term> {
    None
}
